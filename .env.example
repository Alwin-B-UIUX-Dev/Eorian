# Development Environment Configuration Template
# Pour le développement local uniquement
# Documentation: README.md section "Installation"

# Application
NODE_ENV="development"                      # development | production | test
PORT=3000                                   # Port local (prod: variable selon hébergeur)
HOST="localhost"                            # Local only (prod: "0.0.0.0")
LOG_LEVEL="debug"                           # debug | info | warn | error (prod: "info")

# Configuration régionale
TIMEZONE="Europe/Paris"                     # Fuseau horaire pour les dates

# Database (PostgreSQL Local)
DATABASE_URL="postgresql://api_service_eorian:secure_password@localhost:5432/eorian_fr_db"

# Database (PostgreSQL Prod: Utiliser l'URL fournie par l'hébergeur)
# DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# JWT Development (Valeurs NON sécurisées)
JWT_SECRET="dev-secret-change-in-production"    # JAMAIS en production !
JWT_EXPIRES_IN="15m"                            # Durée du token principal
JWT_REFRESH_EXPIRES_IN="7d"                     # Durée du refresh token
JWT_ISSUER="http://localhost:3000/api"          # Émetteur API local
JWT_AUDIENCE="http://localhost:3000"            # Audience application locale

# =====================================
# CHECKLIST AVANT PRODUCTION:
# =====================================
# JWT_SECRET généré aléatoirement (openssl rand -hex 32)
# DATABASE_URL avec SSL activé (?sslmode=require)
# HOST="0.0.0.0" pour Docker/containers
# LOG_LEVEL="info" (pas de debug)
# URLs HTTPS uniquement
