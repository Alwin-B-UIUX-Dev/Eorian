import type { IAddresses } from "@/interfaces/entities";
import type { IBaseService } from "@/interfaces/services/IBaseService";
import type { IAddressesData } from "@/types";

export interface IAdressesService extends IBaseService <IAddresses, IAddressesData>{
    
}