/**
 * interfaces/
 * Rôle:                    déclarer des contrats TypeScript (signatures des services, dépôts, adaptateurs).
 * Fonctionnement:          préciser “ce qui est attendu” sans imposer “comment c’est fait”.
 * But:                     découpler les composants et permettre le remplacement d’implémentations.
 * Bénéfices:               flexibilité, testabilité (mocks), architecture modulaire.
 */